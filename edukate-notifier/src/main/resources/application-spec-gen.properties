# Override springdoc path to a consistent URL used by the generateOpenApiDocs Gradle task
springdoc.api-docs.path=/v3/api-docs

# Dummy values for required env-var placeholders — no real connections are made during spec generation
spring.data.mongodb.uri=mongodb://localhost:27017/spec-gen
spring.rabbitmq.addresses=localhost:5672

gateway.url=http://localhost:5810
